### Generated by Winemaker 0.7.6


SRCDIR                = .
DLLS                  =
EXES                  = rradio



### Common settings

CEXTRA                = -mno-cygwin \
			-m32
CXXEXTRA              = -mno-cygwin \
			-m32
RCEXTRA               =
DEFINES               =
INCLUDE_PATH          = -I../h \
			-I. \
			$(MFC_INCLUDE_PATH)
DLL_PATH              =
DLL_IMPORTS           =
LIBRARY_PATH          =
LIBRARIES             =


### rradio sources and settings

rradio_MODULE         = rradio
rradio_C_SRCS         =
rradio_CXX_SRCS       = rradio.cpp \
			rradioc.cpp \
			rradiop.cpp
rradio_RC_SRCS        = rradio.rc
rradio_LDFLAGS        = -mwindows \
			-mno-cygwin \
			-m32
rradio_DLL_PATH       = $(MFC_LIBRARY_PATH)
rradio_DLLS           = odbc32 \
			ole32 \
			oleaut32 \
			winspool \
			odbccp32 \
			mfc.dll
rradio_LIBRARY_PATH   = $(MFC_LIBRARY_PATH)
rradio_LIBRARIES      = uuid \
			mfc

rradio_OBJS           = $(rradio_C_SRCS:.c=.o) \
			$(rradio_CXX_SRCS:.cpp=.o) \
			$(rradio_RC_SRCS:.rc=.res)



### Global source lists

C_SRCS                = $(rradio_C_SRCS)
CXX_SRCS              = $(rradio_CXX_SRCS)
RC_SRCS               = $(rradio_RC_SRCS)


### Tools

CC = winegcc
CXX = wineg++
RC = wrc


### Generic targets

all: $(DLLS:%=%.so) $(EXES)

### Build rules

.PHONY: all clean dummy

$(SUBDIRS): dummy
	@cd $@ && $(MAKE)

# Implicit rules

.SUFFIXES: .cpp .rc .res
DEFINCL = $(INCLUDE_PATH) $(DEFINES) $(OPTIONS)

.c.o:
	$(CC) -c $(CFLAGS) $(CEXTRA) $(DEFINCL) -o $@ $<

.cpp.o:
	$(CXX) -c $(CXXFLAGS) $(CXXEXTRA) $(DEFINCL) -o $@ $<

.cxx.o:
	$(CXX) -c $(CXXFLAGS) $(CXXEXTRA) $(DEFINCL) -o $@ $<

.rc.res:
	$(RC) $(RCFLAGS) $(RCEXTRA) $(DEFINCL) -fo$@ $<

# Rules for cleaning

CLEAN_FILES     = y.tab.c y.tab.h lex.yy.c core *.orig *.rej \
                  \\\#*\\\# *~ *% .\\\#*

clean:: $(SUBDIRS:%=%/__clean__) $(EXTRASUBDIRS:%=%/__clean__)
	$(RM) $(CLEAN_FILES) $(RC_SRCS:.rc=.res) $(C_SRCS:.c=.o) $(CXX_SRCS:.cpp=.o)
	$(RM) $(DLLS:%=%.so) $(EXES) $(EXES:%=%.so)

$(SUBDIRS:%=%/__clean__): dummy
	cd `dirname $@` && $(MAKE) clean

$(EXTRASUBDIRS:%=%/__clean__): dummy
	-cd `dirname $@` && $(RM) $(CLEAN_FILES)

### Target specific build rules
DEFLIB = $(LIBRARY_PATH) $(LIBRARIES) $(DLL_PATH) $(DLL_IMPORTS:%=-l%)

$(rradio_MODULE): $(rradio_OBJS)
	$(CXX) $(rradio_LDFLAGS) -o $@ $(rradio_OBJS) $(rradio_LIBRARY_PATH) $(DEFLIB) $(rradio_DLLS:%=-l%) $(rradio_LIBRARIES:%=-l%)


